# NBT-Project 필수 라이브러리
# 버전 고정: 2026-03-24 (pip freeze 기준)
# 주의: >= 방식은 메이저 업데이트 시 호환성 문제 발생 가능 → == 고정 유지

# 네트워크 장비 접속
netmiko==4.6.0
paramiko==4.0.0

# 설정 관리
pyyaml==6.0.3
python-dotenv==1.2.2

# CLI
typer==0.24.1

# Web 서버 (v4.1)
# fastapi 0.115.0 고정 → starlette 0.38.6 자동 설치 (1.0.0 호환성 문제 회피)
fastapi==0.115.0
uvicorn==0.42.0
jinja2==3.1.6

# 비동기 작업 큐 (v4.2)
celery==5.6.2
redis==7.3.0

# WebSocket (v4.2)
websockets==16.0

# 로그인 인증 (v4.3)
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4

# ChromaDB (v5.1)
chromadb==1.5.5